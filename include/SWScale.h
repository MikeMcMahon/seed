#pragma once

namespace Util { 
    // Allows the user to specify the sprite should scale in or out 
    typedef struct _SWScale {
        float y;            // 1.0f = 100%
        float x;
    } SWScale; }